# TODO

Tracked improvements and future work for spec-bridge-v2.

---

## Skills

- **Update the `tasks` skill** to generate a `manual-testing-checklist.md` alongside `tasks.md` and the WP files. The checklist should contain enough information to manually test the features added or modified by the spec -- covering happy-path scenarios, edge cases, and any UI or CLI interactions introduced by the work packages.
- improve the review skill so that it works as a code reviewer for implementation and intent resolution 
- implement docs/research/verify
- Can we also make sure the spec-bridge can understand who is asking the question whether its PM or engineer or a business domain expert ? 
- Based on that asks questisons and creates plan but also says these things needs to validated by an engineer some sort ?