modifications to the audit fix skill:
1. Should show code that is referencing in the questions so a person can look at the code while answering the questions, it should be the same output as when asked: "Show me the code that is being referenced"
2. stop the skill for requiring confirmation if one the phrases for the veredicts is used and show yes/no options selectable by the user when asking yes/no questions
3. after every turn give the options in a clickable manner:
intended behavior - current behavior is correct and deliberate
bug - this is a defect that should be fixed with priority but with no large changes
approved improvement - this should be changed and could include larger changes
deferred - valid concern, address later
out-of-scope - explicitly excluded from consideration
2. the audit fix skill should record the guidance given by the user in the questions.md file and the spec should take it into consideration when implementing the fix and if it contradicts the spec creation should be canceled and the question marked as not answered with a notice of why this happened
